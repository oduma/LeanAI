<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:LeanAI.Maui.ViewModels"
    x:Class="LeanAI.Maui.Views.Settings.SettingsPage"
    x:DataType="vm:SettingsViewModel"
    Title="Settings"
    BackgroundColor="{StaticResource ColorBase}">

    <VerticalStackLayout Padding="24,32" Spacing="0">

        <!-- Section header -->
        <Label
            Text="PROFILE"
            TextColor="{StaticResource ColorNickel}"
            FontSize="11"
            CharacterSpacing="2"
            Margin="4,0,0,10" />

        <!-- Re-Run action row -->
        <Border
            BackgroundColor="#2A2A2A"
            StrokeThickness="0"
            Padding="0">
            <Border.StrokeShape>
                <RoundRectangle CornerRadius="10" />
            </Border.StrokeShape>
            <Grid
                Padding="16,14"
                ColumnDefinitions="*,Auto">
                <VerticalStackLayout
                    Grid.Column="0"
                    Spacing="3">
                    <Label
                        Text="Re-Run the Setup"
                        TextColor="{StaticResource ColorText}"
                        FontSize="16"
                        FontAttributes="Bold" />
                    <Label
                        Text="Your stats and your goals will be deleted!"
                        TextColor="{StaticResource ColorNickel}"
                        FontSize="13" />
                </VerticalStackLayout>
                <Label
                    Grid.Column="1"
                    Text="›"
                    TextColor="{StaticResource ColorNickel}"
                    FontSize="22"
                    VerticalOptions="Center" />
                <Grid.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding ReRunSetupCommand}" />
                </Grid.GestureRecognizers>
            </Grid>
        </Border>

    </VerticalStackLayout>

</ContentPage>
